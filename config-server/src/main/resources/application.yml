server:
  port: 8888

spring:
  application:
    name: config-server
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: file:../deploy/config-repo
management:
  endpoints:
    web:
      exposure:
        include: "*"
